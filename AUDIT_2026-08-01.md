# 严格审计 + 深度修复计划（2026-08-01）

> 审计方法：实地进程/日志/结果文件核验 + 直连 BRAIN 权威拉取 + 顶级联网资料双源交叉验证。
> 审计目标：①有无崩溃/死锁（空转）②是否沿用旧架构系统③不同部分的全面深度修复方向。

---

## 0. 你的问题：那"两个因子"是什么、提交成功了吗？

**结论先说：本地有 4 条真实提交（全是 USA/TOP3000），不是 2 条；它们在平台上全是 `UNSUBMITTED`，没有一个进入 `ACTIVE` 活池。**

- `batch_submit_results.json` 含 **4 个带真实 `alpha_id` 的提交**：`O0xJOdrR` / `np8AJ203` / `omgopP95` / `le3MOenn`，全部 USA/TOP3000，仿真 `COMPLETE`。
- 直连 BRAIN（`POST /authentication` → 201，分页拉 `GET /users/self/alphas`）权威确认：
  - 平台全量 **1100 条 alpha，状态 100% `UNSUBMITTED`，`ACTIVE = 0`、`SUBMITTED = 0`、`PENDING = 0`**。
  - 这 4 个 `alpha_id` 在平台上**全部 `UNSUBMITTED`**。
  - 区域分布：USA 543 / IND 276 / EUR 205 / ASI 73 / CHN 3。
- 你面板看到的"两个"应是某个过滤视图（如"今日新增/已提交"计数）；本地真实落地是 4 条 USA。
- **"提交成功"的准确含义**：本地拿到 HTTP 201 = 提交动作成功、alpha 进了平台队列；但平台质量复审**没有放行任何一个到 ACTIVE 活池**。所以严格说：**提交动作发生了，但"成功进入生产"= 没有。** 这与我们此前"1100 条 0 ACTIVE"的结论完全一致。

---

## 1. 有无崩溃 / 死锁（空转）？

**进程没崩、没死锁。** 证据：
- 监督树存活：`start.py`(21632) → `adapter_host`(37664) / `panel_app`(35508, 持 8766 Listen) / `project_runtime`(32988)。
- `project_runtime`(32988) 当前派生 `submit_batch.py`(31700, 13:31 启动)，心跳 `hb_project_runtime` 13:44:50 = `job:submit_batch`，supervisor 13:44:59 新鲜。
- `submit_batch`(31700) 与 BRAIN API 有真实 ESTABLISHED 连接（前次核查 3 条到 `api.worldquantbrain.com:443`），CPU/内存在正常爬升 —— **确确实实在提交，不是假死**。

**但"空转"以另一种形式存在 —— 是逻辑层在大量浪费高质候选，不是进程死了。详见第 3 节。**

---

## 2. 是否沿用旧架构系统？—— **是，且有铁证。**

生产编排器 `project_runtime.py` 把提交脚本排进生产循环：
- 第 101/253 行：`SUBMIT_SCRIPT = submit_batch.py` 作为正式 job 跑；
- 第 103/252 行：`CANDIDATE_SUBMIT_SCRIPT = candidate_submitter.py`（标注"新增"）也作为 job 跑。

### 旧架构残留点（按严重度）
1. **`submit_batch.py` = 纯旧架构 + 硬编码测试批，却在生产循环里跑**：
   - 用裸 `requests.Session` + `HTTPBasicAuth`，**完全没走**我们建的 `core/infrastructure/resilient_http.py`（无熔断器、无重试、无 4xx body 保留）。
   - **完全没过** `submit_gate.py`（无 IND 浓度闸、无 Sub-Universe 闸）。
   - 阈值 `FITNESS/SHARPE=1.5`、`TURNOVER=0.20` 是脚本里**硬编码**，不是验证过的公式。
   - `ALPHAS` 是 **4 条写死的 USA 测试表达式** —— 这是测试批，不是生产研究循环。
   → 当前（13:31 起）生产循环跑的就是它。IND 浓度闸对它**完全不生效**。

2. **`candidate_submitter.py` 同时保留大量 legacy shim 兜底**（`_Resp` / `_safe_json_legacy` / legacy call sites，第 40/94/163/197/224 行等），新架构 `ResilientSession` 只是"优先路径"，旧路径仍作为 fallback 保留。

3. **验证过的 `thresholds.py`（√252 Sub-Universe 公式）在生产路径里是死代码**：
   - `candidate_submitter.py` 第 654 行调 `gate_submission(region, universe, is_checks, is_metrics)` —— **没传 `sub_size`/`largest_universe_size`/`delay`**。
   - `submit_gate.py` 第 143 行 `if sub_size and largest_universe_size:` 恒为假 → Sub-Universe 闸**永不触发**。
   - `candidate_submitter.py` 用**自己的**硬编码 `FITNESS/SHARPE=1.5`、`TURNOVER=0.30`（第 65-69 行），与 `submit_batch.py` 的 0.20 还不统一，且都不等于我们验证过的公式。
   → 即：建了统一闸，但生产路径各写一套阈值，验证过的 Sub-Universe 公式没被接管。

4. **`candidate_generator.py` 第 37 行注释自承**："The REAL gate is in candidate_submitter.py (FITNESS≥1.7, SHARPE≥1.5)" —— 说明生成端压根不依赖统一闸，各脚本各自为政。

---

## 3. "空转"真凶（逻辑层浪费高质候选）—— 已纠偏误诊

`candidate_submit_results.json` 共 77 条仿真：**44 ERROR / 24 COMPLETE / 9 TIMEOUT**。

### 3.1 我纠正了之前的误诊
之前我把"单位不兼容"当可忽略警告。实测 `poll_simulation`（287-320）+ `process_one`（607-610）：BRAIN 对这些表达式**仿真状态返回的就是 `ERROR`**（因为是 `expected Unit[], found Unit[Group:1]` —— **把分组数据喂进了时间序列算子**，是真正的算子不兼容，不是 `close+adv20` 那种标量单位警告）。它们**在仿真阶段就拿不到 `alpha_id` 被丢弃**，到不了提交闸。所以修复点在**生成器**，不是"忽略警告"。

### 3.2 ERROR 拆解（44 条）
- **15 条 `Incompatible unit`（`ts_corr`/`ts_zscore`/`divide` 收到 `Unit[Group:1]`）**：生成器把**分组/横截面字段**直接喂给时序算子。真·生成端类型 bug。这些候选的**父因子质量极高**（source_fitness 1.85~2.86、source_sharpe 1.32~2.39），等于把好端端的高 fitness 变体当废片烧。
- **12 条 `ts_backfill/divide does not support event inputs`**：生成器把 **event 类型数据字段**喂进了不支持它的算子。真·生成端数据类型 bug。
- 其余为其它硬错误。

### 3.3 其它"空转"信号
- **EUR/TOP500 + `truncation 0.12` 泄漏**：`candidate_submit_results` 出现 8 条 EUR 仿真（全 TIMEOUT）且 truncation 高达 0.12 —— 生成器仍在产非目标区域（我们方向定的是 IND/TOP500 + USA/TOP3000）和高截断（浓度风险）的候选，旧配置泄漏。
- **仿真指标没落盘**：COMPLETE 条目本地未存 `sharpe/is_metrics`，研究循环对质量"失明"，无法在本地评估/迭代。
- **IND 至今 0 真实提交**：结果文件里 0 条 IND，alpha_db 大量 `IND/TOP500` 记录 UNSUBMITTED —— IND 卡在 403 浓度闸（与我们之前诊断一致）。

---

## 4. 顶级联网资料交叉验证（支撑审计结论）

- **"Incompatible unit" 性质**：CSDN 实测 + 官方说明 —— 单位不一致通常是**警告、不阻止提交**；但 `Unit[Group:1]→时序算子` 这类是更硬的算子不兼容，BRAIN 仿真直接 `ERROR`。→ 修复在生成器做类型感知，而非忽略。
- **BRAIN 提交硬指标**（gentlecactus 等多源）：Sharpe > 1.25、Fitness > 1、Self-Correlation < 0.7 才进活池；每日分数上限 2000。→ 我们 1100 条 0 ACTIVE = 质量线没过，印证"质量优先于数量"的方向，且本地闸（1.5/1.5）比官方更严，方向对。
- **公式确认**：`√252 × max(0.065, ratio×coeff)`（我们 `thresholds.py`）被独立源结构印证，正确保留；社区另类公式 `0.75×√(sub/uni)×α` 非主流，已正确否决。

---

## 5. 全面深度修复计划（按杠杆排序，STDD 纪律执行）

| # | 修复项 | 类型 | 杠杆 | 风险 |
|---|--------|------|------|------|
| **R1** | 把 `sub_size/largest_universe_size/delay` 传进 `candidate_submitter` 的 `gate_submission`，**激活验证过的 √252 Sub-Universe 公式**（消除死代码） | 接线 | 高 | 低（纯接线+测试） |
| **R2** | 把硬编码测试批 `submit_batch.py` **移出生产 job 循环**（`project_runtime.py` 第 253 行删去），保留文件供手动测试；生产只跑 `candidate_submitter` | 去旧架构 | 高 | 低（可逆） |
| **R3** | **生成器类型感知**：禁止 event 字段→`ts_backfill/divide`、禁止分组字段→时序算子；区域/截断区域感知（IND/TOP500 + USA/TOP3000，trunc 区域化） | 根因 | 最高 | 中（改输出，需测试覆盖） |
| **R4** | 仿真 `COMPLETE` 时**落盘 `is_metrics`/`sharpe`**，让研究循环本地可见质量、可迭代 | 可观测 | 中 | 低 |
| **R5** | `candidate_submitter` 统一阈值来源：本地硬编码 1.5/1.5/0.30 改为引用 `thresholds.py`/统一配置，消除与 `submit_batch` 的不一致 | 一致性 | 中 | 低 |

> 执行纪律：每项 test-first（Red→Green）、过四门 Merge Gate（规格覆盖/三层测试/契约/QA 独立评估+变异分≥70%），写者≠认证者。当前跑着的生产进程改文件不影响，需重启监督树才生效（可逆）。

---

## 6. 本次审计直接回答你的三个问题

1. **有没有崩/空转？** 进程没崩没死锁；"空转"是**逻辑层**在烧高质候选（生成器产非法表达式 27+/44，指标不落盘，IND 0 提交）。
2. **沿用旧架构？** **是**。`submit_batch.py`（旧架构硬编码测试批）在生产循环跑；验证过的 `thresholds.py` 公式是死代码；`candidate_submitter` 自带 legacy shim + 自己一套阈值。
3. **两个因子？** 本地 4 条 USA 真实提交（非 2），平台全 `UNSUBMITTED`、0 `ACTIVE` —— 提交动作成功，但无一进入活池。
