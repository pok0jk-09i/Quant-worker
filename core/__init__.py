"""Quant worker core infrastructure package.

Internal, importable from both the research tree (scripts/) and the
monitor panel (quant_worker_monitor_panel/). Kept dependency-free so it can
run under the locked-down production interpreter.
"""
